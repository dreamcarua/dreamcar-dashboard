# DreamCar Dashboard — traps

Read before the first edit of code, workflow or config. Add an entry whenever something cost more than 15 minutes of surprise, in the same commit as the fix.
Entries below were harvested from commit messages on 03.09.2026 (source: `git log`, commits of 24.08.2026 by "Claude (DreamCar AI)"); each carries the commit date. Text in Ukrainian on purpose — people read this file.

### Назви секретів у GitHub ≠ назви в `.env`
**Symptom:** новий воркфлоу читає `secrets.TELEGRAM_BOT_TOKEN` — порожньо; власника просять поставити секрет, який уже є.
**Cause:** у GitHub Secrets цього репо Telegram-секрети називаються `TG_BOT_TOKEN` і `TG_CHAT_ID` (так їх читає `meta-stats-sync.yml`); `.env.example` легасі-дашборду називає ті самі речі `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID`. Інші наявні секрети: `FB_ACCESS_TOKEN`, `IG_USER_ID`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `GH_TEAM_NOTIFY_TOKEN`. SMM-чат заданий літералом у `smm-content-watchdog.yml` (`SMM_CHAT_ID`).
**Do:** перед тим як просити новий секрет — `grep -h 'secrets\.' .github/workflows/*.yml | sort -u`: список того, що вже є. Нові воркфлоу вживають `TG_BOT_TOKEN` / `TG_CHAT_ID`.
**Seen:** 03.09.2026 · встановлення воркфлоу звітів — помилка Claude, власник зупинив

### Один процес, два канали запуску (ETL MySQL → Supabase)
**Symptom:** ETL стартував 96 разів на добу замість 48; половина з 324 хв/міс CI йшла в нікуди.
**Cause:** `schedule:` у воркфлоу (0,30) і pg_cron job у Supabase через `etl-trigger` (15,45) запускали той самий процес. Кожен ран 21 с, білиться як повна хвилина.
**Do:** перед зміною частоти будь-якого ETL — знайти ВСІ канали: `schedule:` у `.github/workflows/`, pg_cron у Supabase, `workflow_dispatch`, вебхуки. Головний канал — pg_cron; `schedule:` лише резерв 3×/добу зі зсувом :05.
**Seen:** 24.08.2026 · commit 6ef8963

### GitHub cron ходить у 2–4 рази рідше за розклад
**Symptom:** воркфлоу з `cron: */15` реально запускається кілька разів на годину або рідше.
**Cause:** планувальник GitHub Actions придушує й пропускає запуски; вираз `cron:` — не фактична частота.
**Do:** навантаження рахувати з логу ранів (Actions → workflow → runs) або з Billing → Usage, а не з виразу `cron:`. Для критичних за часом процесів — pg_cron.
**Seen:** 24.08.2026 · commit 6ef8963

### Білінг округлює кожен job угору до хвилини
**Symptom:** воркфлоу з 10-секундними холостими ранами коштує стільки ж, скільки хвилинний; `fb-ads-sync` — 1257 хв/міс.
**Cause:** одиниця тарифікації — job-хвилина з округленням угору; вартість визначає кількість ранів, а не тривалість.
**Do:** оптимізувати кількість запусків (нічний throttle, обʼєднання), не швидкість коротких скриптів. Кожен воркфлоу — з `timeout-minutes` (без нього зависла job тримає один із 20 конкурентних слотів акаунта до 6 годин).
**Seen:** 24.08.2026 · commits 1efdb39, 438c6ab

### Дорога підготовка перед перевіркою, чи є робота (FB Ads ETL)
**Symptom:** у міжпромовий період ETL 12 разів на добу тягнув сотні оголошень заради нуля рядків, ~3 хв на ран.
**Cause:** порядок кроків: спершу повний список оголошень (пагінація по 200), потім insights.
**Do:** найдешевша перевірка («чи були покази») — першою; дорогі кроки — лише якщо є що обробляти. Правило дешевого вартового: якщо перевірка не відповіла — виконувати повний цикл, а не пропускати.
**Seen:** 24.08.2026 · commit 0ba6f63

### Назва воркфлоу описує стару поведінку
**Symptom:** «every 15min», «2 iterations × 15 min» у назвах — при cron 0,30 і без internal loop з 08.08.
**Cause:** поведінку змінили, підписи й шапки файлів — ні.
**Do:** міняти назву, шапку файлу й коментарі в тому ж коміті, що й поведінку.
**Seen:** 24.08.2026 · commits a52c726, 6ef8963

### Документ описує інший репозиторій
**Symptom:** корінний `CLAUDE.md` і `README.md` описують legacy PHP `utm-dashboard` на двох серверах; `docs/README.md` каже, що сайт у `web/`, а тека — `docs/`.
**Cause:** репо — clean copy (SECURITY.md) з іншого проєкту; документи переїхали, код — еволюціонував.
**Do:** документ, що суперечить коду, — вір коду й виправ документ у тій самій відповіді. Старий `CLAUDE.md` — у `memory/archive/`, він досі корисний для legacy PHP і вебхуків.
**Seen:** 03.09.2026 · встановлення системи памʼяті

### `gh run list --limit N` віддає останні N ранів, а не всі за період
**Symptom:** оцінка навантаження Actions занижена у 20 разів (356 ранів замість 5854; 31 093 хв за серпень), на цій цифрі ухвалено рішення приватизувати репозиторії — квота вигоріла за 3 дні й зупинила прод.
**Cause:** ключ `--limit` обмежує вибірку, а не період; перед тим та сама метрика через wall-clock `startedAt→updatedAt` завищила на 31% (у різницю входить черга).
**Do:** будь-яка цифра про CI — з Billing → Usage. Форма звіту (розбивка по процесах, динаміка по днях) створює довіру, якої дані не заслуговують: перевіряти першоджерело саме там, де все залежить від однієї цифри.
**Seen:** 24.08.2026 · ✅ репо повернуто в public 27.08

### MV не фільтрує угоди по датах — подвійний рахунок між циклами
**Symptom:** проєкт показує вдвічі більше оплат і виручки, ніж було.
**Cause:** `mv_dashboard_projects_stats` джойнить угоди лише по збігу мітки з `dashboard_projects.deal_project_values`, без `date_start..date_end`. Одна мітка у двох проєктів = ті самі оплати в обох.
**Асиметрія, яка це підсилює:** у `mv_dashboard_projects_stats` угоди джойняться ЛИШЕ по мітці (`d.project = ANY(deal_project_values)`), а рекламний спенд і ручні витрати — ЛИШЕ по вікну `date_start..date_end`. Тобто проєкт може зібрати виручку за межами свого вікна, але ніколи не збере за ті самі дні витрати: ROAS і P&L спотворюються в обидва боки. Гірше, коли `date_start = date_end` (у червневого iPhone-проєкту вікно — один день 06.06): виручка за місяць, спенд за добу.
**Do:** нова мітка на кожен цикл (тригер колізій уже стоїть). Фікс класу помилки = фільтр по датах у MV, але він перерахує історичні цифри всіх проєктів — рішення власника.
**Справжня причина (знайдено 04.09.2026):** мітку ставить не ETL, а тригер `tg_deals_normalize_project` на `dashboard_deals` (BEFORE INSERT OR UPDATE). Він мапив префікс `deal_name` на назву проєкту БЕЗ дати: усе, що `DCI-iphone-%`, ставало `IPHONE 17 PRO MAX`. Липневий цикл продавався з двох префіксів (`DCI-promax-` — новий лендінг, `DCI-iphone-` — невимкнені старі лінки), тож 905 оплат осіли в червні. Наслідок для будь-кого, хто спробує це «полагодити» руками: **UPDATE рядків без правки тригера відкочується тим самим тригером**.
**Виправлено 04.09.2026:** тригер став date-aware (`DCI-iphone-` у вікні 02-05.07 → `IPHONE 17 PRO MAX 2`), 905 pay + 191 pending перемічено, MV перебудовано. Бекап: `public._backup_deals_iphone_relabel_20260904`. Міграція: `etl/migrations/20260904_iphone_cycle_relabel.sql`.
**Детектор:** `public.v_project_label_bleed` показує мітки, що перетікають у вікна інших проєктів (поріг 50 угод). Перевіряти після старту кожного циклу.
**Seen:** 17.08.2026 · ✅ виправлено 04.09.2026

### Зелений статус ≠ корисна робота
**Symptom:** воркфлоу місяцями «успішні» й не роблять нічого (skip через відсутні секрети, 0 рядків, порожні записи).
**Do:** перевірка «хто споживає результат»: що процес мав записати → коли там востаннє зʼявився запис → чи він не порожній. Вимикати через `disable`.
**Seen:** 24.08.2026 · ✅ −662 хв/міс

### Дорога підготовка перед перевіркою, чи є робота
**Symptom:** ETL FB Ads працював 179 с навіть при вимкненій рекламі.
**Cause:** спершу викачувався весь список оголошень акаунта заради `url_tags`, і лише потім insights.
**Do:** insights першими; немає рядків — акаунт пропускається. Стало 15 с.
**Seen:** 24.08.2026 · ✅

### Звіт, який не читається, забирав із собою всі інші
**Symptom:** один поганий файл у `reports/` — і воркфлоу `report-to-telegram` падає, а решта звітів того ж пушу не доходить. У логу лише помилка `jq`.
**Cause:** цикл відправки йшов під `set -euo pipefail` і кликав `jq` без перевірки. Перший файл, що не розпарсився, убивав увесь крок. 14.09.2026 так замовк приватний міст `memory-kit` на добу: агент згенерував base64 **руками** (модель писала його токен за токеном) і поклав у `.json` замість JSON. Ознака: латиниця й цифри декодуються бездоганно, кирилиця — місиво.
**Do:** звіт пишеться звичайним UTF-8 JSON і починається з `{`; кодує інструмент, не модель — вміст, що починається з `eyJ`, уже знищено і не лагодиться. Воркфлоу оновлено 14.09.2026: нечитабельний звіт пропускається з гучним повідомленням у сам канал, недоставлений лишається в `reports/` (дошле повторний `workflow_dispatch`), а червоний run сам пише про себе в чат.
**Seen:** 14.09.2026 · ✅ виправлено

### Новий розіграш без `code`/`deal_aliases` краде alias старого проєкту
**Symptom:** активний розіграш (#21 AUDI Q7 Prestige) у дашборді показував 5 571 лід, але 0 оплат, 0 покупців, 0 ₴ виручки. У БД при цьому 5 893 угоди й 4 789 оплат.
**Cause:** `launches` для #21 мав `code=NULL` і `deal_aliases=NULL`. Auto-merge у `loadProjects()` (docs/index.html) підставив `code = id.slice(0,8)` = `424f1dbc`, а список значень `project` для угод визначав substring-матчингом назви по вибірці `dashboard_deals.select('project').limit(5000)` — БЕЗ `order`, тобто довільні 5000 рядків зі 100k+. Нове значення `AUDI Q7 PRESTIGE` у вибірку не потрапляло, зате там було старе `Q7` (розіграш AUDI Q7, груд. 2025), і умова `c.includes(dp)` дала збіг: `'AUDI Q7 PRESTIGE'.includes('Q7')`. Запит ішов з `project=in.(Q7)` по вересневому вікну → нуль рядків.
**Do:** створюючи розіграш у `/projects/`, одразу заповнювати `launches.code` і `launches.deal_aliases` — це єдине джерело, яке не вгадує. Перевірка з браузера: пропатчити `window.fetch` і подивитись, який `project=in.(…)` реально йде у `/rest/v1/dashboard_deals`. Виправлено 15.09.2026: список `project` береться з `mv_dashboard_filter_options` (повний distinct), fallback-матчинг не чіпає значення, вже закріплені за іншим проєктом, а зворотний напрям вимагає ≥ 4 символів.
**Seen:** 15.09.2026 · аудит дашборду на запит Вадима

### «Останні N днів» давали N+1 день
**Symptom:** «Останні 7 днів» показували spend 116 601 ₴, тоді як сума за 7 київських діб — 105 119 ₴. Розбіжність 11 %, не відтворювалась у SQL.
**Cause:** `applyPreset()` рахував `from = today − N`, `to = today`, а межі inclusive-inclusive → 8 діб для `7d`, 31 для `30d`, 61 для `60d`.
**Do:** для inclusive-діапазону віднімати N−1. Перевіряти пресети не по назві, а порівнянням з SQL за той самий календарний період у `Europe/Kyiv`.
**Seen:** 15.09.2026 · аудит дашборду

### `PROJECT_REF_MAP` ключами по НАЗВІ, а `filters.project` — це code
**Symptom:** success rate на дашборді — фікція: «Всього лідів» рахувалось по всіх проєктах, «Оплачено» — по одному.
**Cause:** `PROJECT_REF_MAP` мав ключі `'MOTORCYCLE'`, `'AUDI E-TRON'` (назви), а `filters.project` тримає `code` (`motorcycle_jun2026`, `audi_e_tron`). Умова не спрацьовувала ЖОДНОГО разу з моменту появи, тож `fetchLeadsCount()` завжди повертав лічильник без фільтра по проєкту. Додатково `ilike '%DCI-audi%'` ловив би ще й `DCI-audiq7`.
**Do:** ключі — `code`; префікс закінчувати роздільником (`DCI-audi-`), інакше він ловить довші slug-и. Якщо мапінгу немає — повертати `null`, а не число з іншим охопленням: інакше знаменник конверсії тихо бреше. NB: `dashboard_webhooks` тримає ~14 днів історії, тож для завершених проєктів лічильник лідів недоступний у принципі.
**Seen:** 15.09.2026 · аудит дашборду

### Account-level fallback виконавця переписує реальну атрибуцію
**Symptom:** у розрізі виконавців витрати fortunatos і artem чергувались по днях: 03–07 і 09.09 — лише fortunatos, 08 і 10–15.09 — лише artem. Ті самі `ad_id`, той самий рекламний акаунт. Ліди при цьому щодня йшли від обох.
**Cause:** `etl/sync_fb_ads.py` → `transform_row()`: коли FB не віддав `url_tags` (збій `/ads`, пагінація, права), спрацьовував last-resort мапінг `ad_account_id → executor_utm_term` і ШТАМПУВАВ на весь акаунт одного виконавця плюс вигадані `utm_source='facebook'`, `utm_medium='cpc'`. В акаунті CLUB UAH (`1057590556523878`) працюють двоє, у таблиці `ads_account_to_executor` він закріплений лише за `artem`. Справжні мітки з `url_tags` — `utm_term=fortunatos, utm_source=meta, utm_medium=post`. Підміна `meta/post → facebook/cpc` ламала ще й фільтр типу трафіку.
**Do:** account-level мапінг застосовний, лише коли в акаунті РІВНО один виконавець; інакше — carry-forward останнього відомого значення по `ad_id` (оголошення виконавця не міняє). Ніколи не вигадувати `utm_source`/`utm_medium`. Детектор: `select ad_id, count(distinct utm_term) from dashboard_ads_data where date_start >= … group by 1 having count(distinct utm_term) > 1`. Виправлено 15.09.2026; бекап зіпсованих рядків — `_bak_ads_utm_20260915`.
**Seen:** 15.09.2026 · питання Вадима «переплутані мітки fortunatos та Артем по витратам?»

### Справжня причина зникнення url_tags — FB 500 на важкій експансії creative
**Symptom:** з 10.09 витрати Fortunatos щодня осідали на Артемі; після першого фіксу лишилось 11 402 ₴ «Артема» з ROAS 38x.
**Cause:** `GET /act_1057590556523878/ads?fields=id,name,url_tags,creative{object_story_spec,asset_feed_spec,...}&limit=200` віддавав **500 Internal Server Error** — FB не витримував важку експансію creative на сторінці у 200 оголошень саме для цього акаунта. Виняток ловився одним `except` і давав `tags_map = {}` для ВСЬОГО акаунта, після чого спрацьовував account-level дефолт. Лог показував це як тихе `⚠ link URLs/tags failed: 500 Server Error`, прогін звітував success.
**Do:** для `/ads` тримати сходи деградації набору полів — останній схід тягне ТІЛЬКИ `id,name,url_tags` (майже ніколи не падає), бо саме url_tags несуть атрибуцію, а URL креативу другорядний. Перевірка: `gh run view <id> --log | grep -E "url_tags|link URLs"` — рядок `✓ resolved N URLs, M ads з url_tags` має бути для КОЖНОГО акаунта з insight-рядками. Окремо: `(#10) pages_read_engagement` у другому pass по постах — інша, нешкідлива помилка (fallback на URL посту), не плутати.
**Seen:** 15.09.2026 · аудит дашборду

### Один рекламний акаунт ≠ один медіабаєр
**Symptom:** таблиця «Виконавець» показувала Артему ROAS 38x при 11 402 ₴ витрат — при тому що всі 14 оголошень лежали в кампаніях з назвою `Fortunatos | DC | Audi Q7 | ...`.
**Cause:** `ads_account_to_executor` мапить `ad_account_id → ОДИН executor_utm_term`. У CLUB UAH (`1057590556523878`) працюють двоє. Будь-який збій url_tags → весь акаунт підписувався одним іменем. Артем у циклі #21 витрат на рекламу не мав узагалі: його 1 097 оплат — органіка, TG і розсилки.
**Do:** виконавця визначати за конвенцією назв кампаній `<Виконавець> | DC | <Модель> | <Темп> | <дата>` через `ads_executor_rules` (патерн ILIKE, priority). Account-level дефолт застосовний, лише коли `ads_account_to_executor.is_exclusive = true`; інакше краще NULL, ніж чуже імʼя. Новий медіабаєр → рядок у `ads_executor_rules`, не у мапі акаунтів.
**Seen:** 15.09.2026 · «все одно якісь витрати у Артема» — Вадим

### Платіжка живе у checkout_events, а не в угоді
**Symptom:** фільтр «Платіжка» ховав ~99 % даних: `pay_provider` заповнений у 79 із 5 934 вересневих угод. WayForPay зник із даних 13.08, Platon — 02.09.
**Cause:** `crm_deals.pay_provider` (MySQL) заповнювався лише колбеками WayForPay і Platon. Чекаут давно маршрутизує між `paygate`, `ibanoplata`, `betatransfer`, `transvoucher`, `lava` і прямими IBAN ФОПів (`monobank_*`, `privat_*`, `otp_*`, `ukrsib_*`, `oschad_*`, `pumb_*`) — жоден із них у CRM-угоду шлюз не пише. Але НАШ трекер чекауту пише: `checkout_events.meta = {"gateway": "...", "order_id": "DCI-..."}`, де `order_id` = `order_reference` = перша частина `deal_name` до ` - `.
**Do:** джерело правди по платіжці — `checkout_events.meta->>'gateway'`, join по `split_part(deal_name,' - ',1)`. Тригер `tg_dashboard_deals_pay_provider` ставить її на кожен upsert, `reconcile_pay_provider()` (pg_cron, щогодини :17) добирає випадки, коли подія чекауту прийшла після угди. Ручні мітки операторів не перетираються. Покриття після бекфілу: 93,6 % оплачених угод вересня.
**Seen:** 15.09.2026 · «Платіжка — заповнюй» — Вадим

### Витрати не звужувались по проєкту — ROI брехав при перетині циклів
**Symptom:** вибираєш розіграш — виручка звужується, spend лишається за весь період.
**Cause:** `dashboard_ads_data` не мала колонки `project`, і `adsBaseRange()` обмежував лише дати.
**Do:** колонка `dashboard_ads_data.project` ставиться тригером `tg_dashboard_ads_stamp_project` через `resolve_ads_project(date_start)` — вікна беруться з `v_project_windows` (dashboard_projects + launches, lifetime-проєкти ≥ 2030 ігноруються). На фронті — `narrowAdsByProject(q)`. Виняток навмисний: для UTM-полів (`utm_source/medium/campaign/term/content`) бік угод у `aggViaRPC` теж не звужується по проєкту, тож і витрати там не звужуємо — інакше ROAS рахувався б із різних охоплень.
**Seen:** 15.09.2026 · «витрати не фільтруються по проєкту — виправляй» — Вадим

### Знаменник конверсії не звужується разом із чисельником
**Symptom:** фільтр «Нові клієнти» давав 335 оплат при 5 570 лідах — success rate 6,0 % замість справжніх 78,6 %.
**Cause:** «Оплачено» рахується з `dashboard_deals` (усі фільтри застосовані), а «Всього лідів» — з `dashboard_webhooks`, який знає ЛИШЕ дату й проєкт. Будь-який фільтр рівня угоди (статус, тип клієнта, тариф, платіжка, воронка, тип трафіку, UTM) звужував чисельник і не чіпав знаменник.
**Do:** `fetchLeadsCount()` повертає `null`, щойно активний хоч один фільтр рівня угоди (список `DEAL_LEVEL_FILTERS`), і знаменником стає та сама відфільтрована множина угод. Правило загальне: два KPI з РІЗНИХ таблиць не можна ділити одне на одне, поки на обидві не накладено однакове звуження. Перевірка будь-якого нового фільтра: увімкнути його і звірити обидва числа з SQL за той самий період.
**Seen:** 15.09.2026 · аудит дашборду

### Автовибір проєкту ставив селект, але не filters.model — і це кидало блокуючий alert
**Symptom:** пресети «Весь проєкт», «Перші 3 дні», «Перший тиждень», «Останній тиждень», «Останній день» показували alert «Спершу обери Активний розіграш», хоча розіграш у дропдауні вибраний, і мовчки скидали період на «Весь період» (2019-01-01).
**Cause:** `autoSelectNewestProject()` виставляв `filters.project` і ЗНАЧЕННЯ селекта `#f-model`, але не `filters.model`. `getActiveModel()` читає саме `filters.model` → null → гілка помилки. Гірше за саму помилку: `alert()` блокує весь JS-потік вкладки, тож дашборд завмирав до кліку мишею (на цьому зависла й автоматична перевірка фільтрів).
**Do:** зміна фільтра ніколи не має викликати `alert()` — лише `console.warn` і тихий фолбек; alert лишається тільки на явних діях користувача (збереження, експорт). Стан UI і стан `filters` ставити разом, одним місцем. Пресети мають друге джерело проєкту через `getSelectedProject()`, а `m.end = null` (ongoing) більше не дає Invalid Date.
**Seen:** 15.09.2026 · аудит дашборду

### P&L ідентифікував витрати парою (date_start, spend)
**Symptom:** латентно — на поточних даних цифри правильні, бо цикли не перетинаються.
**Cause:** у `dashboard_project_pnl()` CTE `ads_explicit_keys` робив `DISTINCT date_start, spend`, а `ads_unmatched` виключав будь-який рядок із таким самим `(date_start, spend)`. Два різні оголошення з однаковою сумою за один день склеювались, і чужі витрати тихо випадали з періодної атрибуції. Плюс `ads_explicit` шукав alias у назві кампанії: для циклу #21 alias `AUDI Q7 PRESTIGE` не збігається з `Fortunatos | DC | Audi Q7 | ...`, тож ЖОДНЕ оголошення не матчилось явно — усе йшло у рівний розподіл по датах.
**Do:** ключ рядка — `dashboard_ads_data.id`, перший крок атрибуції — колонка `project` (тригер `tg_dashboard_ads_stamp_project`), далі alias у назві, і лише потім рівний розподіл. Після будь-якої зміни цієї функції звіряти `ad_spend` по всіх циклах ДО і ПІСЛЯ: на непересічних циклах числа мають лишитись ідентичними.
**Seen:** 15.09.2026 · аудит дашборду

### Тисячі рядків очікуваної помилки ховають одну справжню
**Symptom:** FB 500 на `/ads` для CLUB UAH прожив у логах невідомо скільки прогонів непоміченим.
**Cause:** резолв URL через пости падав з `(#10) pages_read_engagement` на кожному з ~900 викликів за прогін і друкував по два рядки на кожен. Справжня помилка — один рядок серед двох тисяч.
**Do:** помилку, що є властивістю системи (немає права), логувати ОДИН раз і вимикати відповідну гілку до кінця прогону — ключем тієї сутності, якої стосується право (тут `page_id`, а не весь прогін). Побічний зиск: ~90 с CI на прогін, а прогонів 12+ на добу.
**Seen:** 15.09.2026 · аудит дашборду

### requests.HTTPError не несе коду помилки Meta
**Symptom:** детектор «немає прав» (`(#10)`) у `_fetch_post_url` виглядав правильним, але не спрацював ЖОДНОГО разу: прогін і далі робив ~460 марних викликів.
**Cause:** `r.raise_for_status()` кидає `HTTPError` з текстом `400 Client Error: Bad Request for url: ...` — ані коду Meta, ані тіла відповіді там немає. Тіло друкувалось окремим рядком `❌ FB 400: {...}` у `fb_get`, і до виклика не доходило. Перевірка `'(#10)' in str(e)` завжди була False.
**Do:** `fb_get` пришиває `meta_error_code=<code>` і початок тіла до тексту винятку. Будь-яка логіка «ця помилка не минеться сама» (права, неіснуючий обʼєкт) має дивитись на КОД Meta, а не на HTTP-статус: 400 однаково повертається і для тимчасового збою, і для відсутнього права. Після фіксу прогін ETL: 261 рядок логу замість 1 059, ~59 с замість ~100 с, при тих самих 856 url_tags.
**Seen:** 15.09.2026 · аудит дашборду — перший фікс виглядав робочим, але не міняв нічого

### Нова таблиця у public = відкрита назовні, поки не увімкнеш RLS
**Symptom:** створив `ads_executor_rules` і бекап `_bak_ads_utm_20260915` звичайним `create table` — обидві одразу читалися ключем із JS публічного сайту.
**Cause:** Supabase роздає новоствореним таблицям у `public` гранти `anon` (SELECT) і `authenticated` (SELECT/INSERT/UPDATE/DELETE) автоматично, через ALTER DEFAULT PRIVILEGES. RLS при цьому НЕ вмикається. Тобто `create table` у public — це публікація, а не приватна дія. Та сама діра знайшлась у бекапі від 04.09 і в бекапі cron-джоб — тобто це не одноразова помилка, а звичка.
**Do:** кожен `create table` у `public` завершувати двома рядками: `alter table ... enable row level security;` і `revoke all on ... from anon, authenticated;`. Для бекапів і службових таблиць політики не потрібні взагалі — RLS без політик = deny all, а ETL ходить під service_role, який RLS обходить. Перевірка після будь-якого DDL: `mcp__Supabase__get_advisors(type='security')` → лінти `rls_disabled_in_public` і `security_definer_view`.
**Seen:** 15.09.2026 · аудит дашборду, перевірка власної роботи

### View у Postgres за замовчуванням має права ВЛАСНИКА
**Symptom:** `v_project_windows`, створений цією ж сесією, вилетів у лінт `security_definer_view` (ERROR).
**Cause:** view виконується з правами того, хто його створив, а не того, хто читає. `v_project_windows` читає `dashboard_projects` і `launches` — обидві під RLS. Відкритий `anon`-у, він віддавав би їх повністю, в обхід політик: RLS базових таблиць просто не застосовується.
**Do:** будь-який новий view у `public`, що читає таблиці під RLS, створювати з `security_invoker = true` (PG15+) або не давати на нього грантів взагалі. Якщо view потрібен лише SQL-функціям — другий варіант чистіший.
**Seen:** 15.09.2026 · аудит дашборду

### Одноразовий pg_cron без року перезапускається щороку
**Symptom:** 23 джоби `verify_pub_*` з розкладами на 5–15 вересня, усі в минулому.
**Cause:** cron-вираз має пʼять полів — хвилина, година, день, місяць, день тижня. Року в ньому НЕМАЄ. «Одноразова» джоба на 9 вересня 16:03 — це насправді щорічна. Код чистить її при спрацюванні, але якщо спрацювання пропущене або впало, вона лежить до наступного року й тоді робить те, що вже не має сенсу (тут — 23 торішні «Час публікації!» у SMM-групу).
**Do:** для справді одноразової дії не покладатись лише на самоприбирання у коді. Періодично перевіряти: `select jobid, jobname, schedule from cron.job where schedule ~ '^[0-9]+ [0-9]+ [0-9]+ [0-9]+ \*$'` — це і є «одноразові», що насправді щорічні. Прибирати ті, чия дата минула.
**Seen:** 15.09.2026 · аудит дашборду (61 таку джобу вже прибирали 04.09 — накопичились нові)

### Я поклав прод: per-row тригер на dashboard_deals вбив ETL на 2 години
**Symptom:** 15.09.2026 о 19:27 прилетів алерт «Revenue завис (2h нуль)» з порадою негайно вимкнути всю рекламу. Насправді продажі йшли — трекер чекауту за ті самі 2 години показував 166 подій і 46 оплат. Замерз ETL MySQL: `dashboard_deals` стояла з 18:30.
**Cause:** мій же тригер `tg_dashboard_deals_pay_provider` (BEFORE INSERT OR UPDATE), доданий того ж дня, робив на КОЖЕН рядок пошук у `checkout_events`. Запит мав `ORDER BY ce.ts DESC LIMIT 1` — і саме це вбивало: планувальник обирав індекс по даті (`ce_ts`) замість мого партіального індексу по `order_id` і сканував назад до першого збігу. Для `order_id`, якого в трекері немає (старі угоди, органіка, TG), збігу немає ніколи → скан усіх 165k рядків.

| lookup | час |
|---|---|
| order_id є у трекері | 0,14 мс |
| order_id немає | **8 977 мс** |
| той самий запит без `ORDER BY ts DESC` | 0,14 мс |

Пачка з 27 угод — і PostgREST-upsert падав з `57014 canceling statement due to statement timeout`. Прогін ETL о 16:30 UTC впав, наступний за розкладом аж о 17:30.
**Do:** на таблицю, у яку пише ETL, НЕ вішати тригер із запитом до іншої великої таблиці. Заповнення поля з іншого джерела робити set-based job-ом (`reconcile_pay_provider()`, pg_cron `*/10`, 43 мс на прогін) — він не має гарячого шляху й не може покласти імпорт. Окремо: `ORDER BY <інша колонка> DESC LIMIT 1` систематично відмовляє від селективного індексу на користь індексу сортування; якщо потрібен «будь-який збіг» — не писати ORDER BY взагалі і перевіряти план EXPLAIN саме на випадку БЕЗ збігу, а не на щасливому.
**Seen:** 15.09.2026 · власна помилка, знайдена через алерт користувача

### Алерт міряв виручку по таблиці й не питав, чи вона взагалі свіжа
**Symptom:** той самий інцидент. `anomaly-alerter` побачив 0₴ за 2 години при ads spend 19 516₴ і надіслав «негайно призупинити всі рекламні кампанії». Рада коштувала б вечора продажів на живому трафіку.
**Cause:** дві незалежні помилки. (1) Алерт читав ЛИШЕ `dashboard_deals` і не перевіряв `max(created_at)` — тож «немає продажів» і «немає даних» для нього одне й те саме. (2) Текст першого кроку писала Haiku з промптом «поверни дію, яку робити першим кроком» — без жодної заборони на дії з грошима, тож модель радила незворотне.
**Do (зроблено у v6):** будь-який алерт по метриці з таблиці спершу перевіряє свіжість тієї таблиці; якщо дані застарілі — це алерт про пайплайн, з іншим текстом і окремим ключем дедупу. Де є незалежне джерело (`checkout_events` пише сам сайт, повз ETL) — звіряти й писати відповідь прямо в алерті. І системний промпт будь-якого бота, чий текст читає людина в паніці, має явно забороняти радити зупинку/паузу/зниження бюджетів: рішення про гроші — тільки людина. Джерело функції тепер у git: `etl/edge/anomaly-alerter/index.ts` (раніше жила лише в Supabase, у жодному репо її не було).
**Seen:** 15.09.2026 · «це що таке??» — Вадим, зі скріншотом алерта

### «Унікальний покупець» рахувався чотирма різними способами
**Symptom:** на маршруті «Аналітика» два тайли на ОДНОМУ екрані показували 3 654 і 3 651 покупців для того самого періоду й проєкту.
**Cause:** чотири незалежні визначення, що розповзлись за рік: `dashboard_kpi_summary` — distinct email (порожній рядок ставав окремим покупцем); `dashboard_kpi_with_delta` — `COALESCE(email, phone)` без `NULLIF`, тож '' теж окремий покупець; `dashboard_extended_kpi` — лише непорожній email і лише серед `paid_at IS NOT NULL`, тож губив тих, хто заплатив без email; фронтенд `renderOverview` — свій `new Set(...)` по email. У циклі #21 троє заплатили без email, але з телефоном — звідси різниця в 3.
**Do:** ключ покупця один на всю систему: `COALESCE(NULLIF(customer_email,''), NULLIF(customer_phone,''))`. Перед тим як додавати новий KPI «унікальні X», грепнути `COUNT(DISTINCT` по всіх функціях і `new Set(` по фронту — інакше зʼявиться пʼяте визначення. Після зміни RPC обовʼязково `delete from dashboard_rpc_cache;`, бо `*_cached` варіанти віддаватимуть старі числа. NB: `mv_dashboard_globals` досі рахує по email — це all-time глобальні числа іншого охоплення, вони не стоять поруч і не конфліктують.
**Seen:** 15.09.2026 · звірка маршрутів з SQL

### Час у таблицях малювався по браузеру, агрегати — по Києву
**Symptom:** угода, створена о 20:28 за Києвом, у Таблиці показувалась як 19:28 і не сходилась із SendPulse.
**Cause:** `fmtDate` і `fmtDateTime` викликали `toLocaleString('uk-UA', …)` БЕЗ `timeZone`, тобто віддавали час браузера. Усе інше в дашборді явно київське (`ymd` з `timeZone: 'Europe/Kyiv'`, `kyivIsoStart/End`, київські добові межі в RPC). Вадим у Польщі (CEST, UTC+2) бачив усе на годину раніше за Київ (EEST, UTC+3); біля півночі розійшлась би й ДАТА — рядок з 21.09 у звіті за 22.09. Двоє людей із різних країн бачили різний час однієї угоди.
**Do:** у проєкті з єдиною бізнес-таймзоною жоден `toLocale*` не можна лишати без `timeZone`. Перевірка: `grep -n "toLocale" | grep -v timeZone` має бути порожнім. Виправлено в одному місці — обидві функції дістали `timeZone: 'Europe/Kyiv'`, це 9 місць використання. NB: експорт CSV віддає сирий ISO зі зсувом (`…+00:00`) — це навмисно однозначно для Excel, але відрізняється від того, що на екрані.
**Seen:** 15.09.2026 · звірка маршруту «Таблиця» з SQL

### Розрізи по UTM навмисно НЕ звужуються по розіграшу
**Symptom:** на маршруті «Виконавець» із вибраним Audi Q7 цифри на 2 угоди більші за SQL по цьому проєкту.
**Cause:** не помилка. `aggViaRPC` для полів `utm_source/medium/campaign/term/content` свідомо скидає `p_project_values` (рішення 08.06.2026: «кампанії й виконавці перетинають проекти»). Тому «Виконавець», «Джерела», «Тип трафіка», «Оголошення», «Комбінації» показують УСІ проєкти за вибраний період, а «Кампанії», «Огляд», «Таблиця», «Ads» — лише вибраний. Різниця у 2 угоди = 1 AUDI E-TRON + 1 BMW X6M, що потрапили у вікно 03–27.09.
**Do:** памʼятати цю асиметрію під час звірок: розбіжність на кілька угод між «Виконавець» і «Огляд» — очікувана, а не поломка. Якщо колись здаватиметься багом — спершу порахувати SQL БЕЗ фільтра проєкту.
**Seen:** 15.09.2026 · звірка маршрутів з SQL

### UTM-фільтри мовчки ігнорувались на половині розділів
**Symptom:** вводиш `utm_term=fortunatos`, лічильник фільтрів показує «4», а «Аналітика» далі малює 5 908 лідів і 1 926 199 ₴ — тобто все. На «Огляді» той самий фільтр дає правильні 740 / 625 / 230 195 ₴.
**Cause:** два різні шляхи даних. Маршрути, що читають угоди порядково через `fetchDealsRange` (Огляд, Кампанії, Таблиця), застосовують `utm_*` через `ilike`. Маршрути на серверних агрегаціях (`dashboard_kpi_with_delta`, `dashboard_extended_kpi`, `dashboard_agg_deals_with_traffic`) приймають лише `project/customer_type/tariff/pay_provider/traffic_type` — **жодного UTM-параметра**. Фільтр просто нікуди не доїжджав, без жодної ознаки для користувача.
**Do:** щоразу, коли у фільтр-барі зʼявляється нове поле, перевірити ОБИДВА шляхи: `fetchDealsRange` і сигнатуру RPC. Якщо RPC його не вміє — не лишати мовчки: або додати параметр, або показати банер. Зараз стоїть банер (`RPC_ONLY_SECTIONS` у docs/index.html); правильний фікс — UTM-параметри в самих RPC — у tasks.md.
**Seen:** 15.09.2026 · звірка фільтрів з SQL

### Тижневі мітки на графіках були неділями замість понеділків
**Symptom:** гранулярність «Тиждень» для періоду 03.09–15.09 давала підписи 30.08 / 06.09 / 13.09. Понеділки цих тижнів — 31.08 / 07.09 / 14.09.
**Cause:** у `bucketKey` понеділок рахувався правильно, але ключ повертався через `tmp.toISOString().slice(0,10)`. `tmp` — це ЛОКАЛЬНА північ браузера, тож у будь-якій зоні з додатним зсувом ISO-серіалізація відкидає дату на день назад: локальна північ 31.08 у Польщі (UTC+2) — це `2026-08-30T22:00:00Z`. Групування при цьому було правильне (зсув однаковий для всіх днів тижня), брехав лише підпис — але у звіті це виглядає як інший тиждень.
**Do:** якщо дату порахували локальними методами (`getDay`, `setDate`), і ключ треба як `YYYY-MM-DD` — збирати його з `getFullYear/getMonth/getDate`, а не через `toISOString()`. Змішувати локальні обчислення з UTC-серіалізацією не можна ніколи. Перевірка: `TZ=Europe/Warsaw node -e '...'` — прогнати всі дні тижня і звірити з `date_trunc('week')` у Postgres.
**Seen:** 15.09.2026 · звірка гранулярності з SQL

### «Платний трафік» рахувався чотирма способами — і реклама виглядала втричі кращою
**Symptom:** дашборд показував ROAS 11,70× по циклу #21. Реальний — 2,63×.
**Cause:** чотири незалежні визначення платного трафіку, кожне у своєму місці:

| Де | Логіка | Платна виручка #21 | ROAS |
|---|---|---|---|
| `is_paid_deal()` — 7 RPC | збіг `utm_campaign/content/term` з `mv_paid_signatures` | 1 649 361 ₴ | 10,02× |
| `isPaidSource()` — фронт | `utm_source` ∈ соцмережі | 1 050 677 ₴ | 6,40× |
| `agg_deals_with_traffic_core` | гардкод імен (`vira`,`artem`…) + список медіумів + сигнатури | своє | своє |
| `mv_dashboard_utm_agg` | той самий CASE, запечений у matview | своє | своє |

Сигнатурний підхід зараховував до платного будь-яку органіку з тією ж міткою креативу: поставили ту саму позначку на IG-сторіс з акаунту — і вона рахувалась як реклама. `isPaidSource` зараховував усе з `utm_source=instagram`, тобто і сторіс, і посилання в біо. Гардкод імен ламався щоразу, як зʼявлявся новий медіабаєр — `fortunatos` у тому списку немає.
**Do:** канон (рішення Вадима 15.09.2026) — **placement-мітка Meta у `utm_medium`**: Meta підставляє `{{placement}}` лише для ОПЛАЧЕНИХ показів, тож `instagram_feed`/`instagram_stories`/`instagram_reels`/`facebook_mobile_*` — реклама, а bare `stories`/`post`/`account`/`telegram_bot`/`newsletter` — органіка. Одна SQL-функція `is_paid_placement(text)` і одна регулярка на фронті (`PAID_PLACEMENT_RE`), які міняються ЛИШЕ разом. Перед додаванням будь-якої метрики про платний трафік: `grep -i "is_paid\|PAID_SOURCES\|paid_signatures"` по функціях, matview і фронту — інакше зʼявиться пʼяте визначення. `mv_paid_signatures` лишилась без споживачів — прибрати разом із її кроном після тижня роботи на нових числах.
**Seen:** 15.09.2026 · звірка «тип трафіку» проти SQL

### Перебудова matview губить індекси й ламає CONCURRENTLY-крон
**Symptom:** `REFRESH MATERIALIZED VIEW CONCURRENTLY` падає після зміни визначення matview.
**Cause:** `CONCURRENTLY` вимагає УНІКАЛЬНОГО індексу. Щоб змінити CASE всередині matview, її треба `drop` + `create` — і разом із нею зникають усі індекси, зокрема унікальний. Крон рефрешу після цього падає мовчки, а дані застигають.
**Do:** перед `drop materialized view` зняти `select string_agg(indexdef,';;') from pg_indexes where tablename='<mv>'` і відновити всі індекси одразу після `create`. Крон на час перебудови зняти (`cron.unschedule`) і перестворити після, інакше він спрацює на половині шляху. Для `mv_dashboard_utm_agg` це: unique btree `(field, key, project, day, tt)` + brin `(day)`, крон `mv-utm-agg-refresh-15min` `'2,32 * * * *'`.
**Seen:** 15.09.2026 · зміна класифікації трафіку

### SQL-функція з `SET search_path` не інлайниться — і коштує 7 µs на кожен рядок
**Symptom:** після додавання 5 UTM-предикатів у RPC `dashboard_kpi_summary` впав з 18.8 мс до 140 мс, `dashboard_kpi_with_delta` — з 59 до 504 мс. Логіка проста, індексів не змінювали, рядків стільки ж.
**Cause:** предикати були винесені у хелпер `utm_eq(col, p)` — SQL, IMMUTABLE, одна строчка. Але з клаузою `SET search_path`. Планувальник Postgres НЕ інлайнить SQL-функцію, у якої є SET-клауза: замість підстановки виразу у WHERE він робить справжній виклик на кожен рядок. 6 000 рядків × 5 предикатів = 30 000 викликів. Те саме виявилось у `is_paid_placement(utm_medium)` (створена 20260915g з `SET search_path`) — вона викликається на кожному рядку у 7 RPC.
**Do:** для маленьких IMMUTABLE-хелперів, які йдуть у WHERE, `SET search_path` НЕ ставити (вони не SECURITY DEFINER і ні до чого не звертаються — підміняти нічого). Для зовнішніх SECURITY DEFINER RPC — ставити обов'язково, там інлайнінг ні до чого. Перевірка: `EXPLAIN` показує виклик функції у Filter замість розкритого виразу; або просто заміряти до/після.
**Seen:** 15.09.2026 · міграція 20260915h · після зняття SET: kpi_summary 12.7 мс, kpi_with_delta 52.0, traffic_type_summary 64.8 → 23.8

### `REVOKE ... FROM PUBLIC` не знімає Supabase-івські автогранти anon/authenticated
**Symptom:** після `DROP FUNCTION` + `CREATE FUNCTION` внутрішня `dashboard_agg_deals_with_traffic_core` (яка навмисно закрита і доступна лише через RLS-обгортку) знову стала викликаваною роллю `anon` — попри `REVOKE ALL ... FROM PUBLIC` у тій самій міграції.
**Cause:** у Supabase на схемі `public` стоїть `ALTER DEFAULT PRIVILEGES ... GRANT EXECUTE ON FUNCTIONS TO anon, authenticated, service_role`. Ці гранти видаються ролям поіменно, а не через PUBLIC, тож `REVOKE FROM PUBLIC` їх не торкається. Та сама вада, що з автогрантами на нові таблиці (запис від 15.09.2026 про `ads_executor_rules`).
**Do:** після кожного `CREATE FUNCTION` у `public`, який має бути закритим — `REVOKE ALL ON FUNCTION ... FROM PUBLIC, anon, authenticated;` і перевірити `select proacl from pg_proc where proname=...`. Очікуваний вигляд закритої функції: `{postgres=X/postgres,service_role=X/postgres}`.
**Seen:** 15.09.2026 · міграція 20260915h

### `Number(null) === 0` — і NULL «немає з чим порівняти» стає впевненим «+0%»
**Symptom:** Аналітика показувала «▲ +0%» («без змін») на розділі, де попередній період порожній. Audi Q7 (старт 03.09) + `utm_term~fortunatos`: 749 замовлень, 232 290 ₴ і три «+0%» поспіль.
**Cause:** `dashboard_kpi_with_delta` навмисно повертає NULL, коли попередній період має менше 5 записів — саме щоб не малювати безглузду дельту. Фронт мав правильний гард `v == null ? '' : ...`, але виклик був `dlt(Number(k.total_delta))`, а `Number(null)` дорівнює 0. Гард ніколи не спрацьовував.
**Do:** серверний NULL до JS-числа приводити лише після перевірки на null. Шукати шаблон `Number(x.something_nullable)` — `Number(null)`, `Number('')` і `Number(false)` усі дають 0, тобто мовчки перетворюють «невідомо» на «нуль».
**Seen:** 15.09.2026 · коміт 9649013

### Фільтр застосували до виручки, але не до витрат — і ROAS порахувався з різних охоплень
**Symptom:** розділ «Виконавець» з фільтром `utm_term~fortunatos` показував другий рядок `vadym`: 0 лідів, 0 виручки, 102 041 ₴ витрат. Підсумковий ROAS по екрану падав.
**Cause:** виручка йде з `dashboard_deals` через RPC (з 20260915h розуміє UTM), витрати — окремим запитом до `dashboard_ads_data`, який звужувався лише по датах і проєкту. Будь-який новий фільтр треба додавати В ОБИДВА боки, інакше чисельник і знаменник рахуються по різних множинах.
**Do:** додаючи фільтр, пройтись по всіх 5 місцях, де читається `dashboard_ads_data` (`grep -n "dashboard_ads_data" docs/index.html`), і застосувати той самий предикат. Зараз для цього є `narrowAdsByProject()` і `narrowAdsByUtm()`.
**Seen:** 15.09.2026 · коміт 0ddd696

### Одне й те саме число на різних екранах — фронт матчить підрядком, сервер матчив рівністю
**Symptom:** `utm_medium=instagram` давав на Огляді 1 055 лідів, а на Аналітиці — 0.
**Cause:** фронт для порядкових розділів робить `q.ilike('utm_x', '%' + value + '%')` (підрядок, без регістру), а перша версія серверного `utm_eq()` порівнювала на рівність. Обидві поведінки «правильні» окремо — разом вони дають два різні числа на одному фільтрі.
**Do:** переносячи клієнтський фільтр на сервер — спершу прочитати, ЯК саме він матчить на клієнті (`grep -n "ilike\|\.eq('" docs/index.html`), і повторити семантику дослівно, а не «як логічніше».
**Seen:** 15.09.2026 · коміт a79d878


### Логін-гейт на фронтенді нічого не захищає — перевіряй публічним ключем
**Symptom:** на дашбордах є overlay «Увійти через Google», роль-чек CEO/COO/Lead, SSO-міст. Виглядає як закрита система. Насправді виручку, P&L і chat ID Telegram міг прочитати будь-хто.
**Cause:** публічний (anon) ключ Supabase за визначенням лежить у коді сторінки. Логін-гейт живе у JS і до бази стосунку не має. Дані закриває лише те, що роль `anon` МОЖЕ у Postgres. У цьому проєкті anon мав явний `anon=r` на ~90 таблицях і `EXECUTE` на 105 SECURITY DEFINER функціях — тобто `dashboard_kpi_summary` віддавала повну виручку без жодної сесії. Табличні дані рятувала RLS (порожній масив), але матв'ю RLS не мають взагалі, а SECURITY DEFINER функції її обходять за визначенням.
**Do:** перевіряти доступ ЗАВЖДИ від імені `anon`, а не «чи відкривається сторінка». Найшвидше — з консолі самої сторінки: взяти `window.supabase.supabaseKey`, зробити `fetch` з цим ключем у заголовках `apikey` і `Authorization` (без сесії) і подивитись статус. 200 = діра. Перед тим, як щось відрізати, підняти логи за добу: `select log_attributes['request.sb.jwt.authorization.payload.role'], log_attributes['request.path'], count(*) from logs where source='edge_logs' group by 1,2` — видно, чи роллю anon у проді хтось реально ходить.
**Seen:** 16.09.2026 · міграція 20260916a · anon мав 0 легітимних запитів за добу, крім /auth/v1/token

### Supabase ALTER DEFAULT PRIVILEGES відрощує права anon на кожному новому об'єкті
**Symptom:** закрив доступ — за тиждень він знову відкритий на нових таблицях і функціях. `REVOKE ... FROM PUBLIC` не допомагає.
**Cause:** у Supabase на схемі `public` стоїть `ALTER DEFAULT PRIVILEGES ... GRANT ... TO anon, authenticated, service_role`. Гранти видаються ролям ПОІМЕННО, тож `REVOKE FROM PUBLIC` їх не бачить. Функції при цьому мають ДВА джерела доступу одразу: явний `anon=X` і успадкований `=X` від PUBLIC — знімати треба обидва.
**Do:** одноразово зняти anon із самих дефолтів: `ALTER DEFAULT PRIVILEGES FOR ROLE postgres IN SCHEMA public REVOKE ALL ON TABLES FROM anon;` і те саме для SEQUENCES та FUNCTIONS (+ `REVOKE EXECUTE ON FUNCTIONS FROM PUBLIC`). Знімаючи PUBLIC з функцій, спершу видати явний грант `authenticated`/`service_role` там, де доступ тримався ЛИШЕ на PUBLIC, інакше зламається те, що працювало. Функції з розширень (`pg_depend.deptype='e'`) не чіпати — у них PUBLIC частина контракту.
**Seen:** 16.09.2026 · міграція 20260916a · третій раз на ті самі граблі за дві доби

### Blended ROAS під підписом «ROAS»
**Symptom:** перший екран дашборду показував ROAS 11,72× і CPL 29 грн, коли реальна ефективність реклами була 2,63× і 127 грн. Ціль «ROAS 5+» виглядала перевиконаною вчетверо.
**Cause:** блок «ЕКОНОМІКА» ділив УСЮ виручку періоду на рекламні витрати. У чисельнику сиділа органіка — TG-канал, IG-акаунт, ретеншн-розсилки — яка реклами не коштувала. Формула математично правильна (це blended ROAS), підпис — ні.
**Do:** будь-яка метрика «щось / рекламні витрати» має у чисельнику МАТИ лише те, що цією рекламою куплене. Якщо потрібні обидві — показувати двома блоками з явними підписами «реклама» і «з органікою», а не одним числом. Той самий принцип уже зафіксовано для фільтрів: звужуєш виручку — звужуй і витрати.
**Seen:** 16.09.2026 · коміт 44aafd5

### Мапа підписів без ключа мовчки дає «undefined» у заголовку
**Symptom:** при виборі гранулярності «Година» картка на Огляді називалась «Угоди undefined».
**Cause:** `{ day:…, week:…, month:… }[filters.granularity]` — ключа 'hour' у мапі не було, хоча кнопка «Година» існувала і `bucketKey` годину рахував правильно. Об'єкт-мапа на невідомий ключ не кидає помилку, а повертає undefined, і воно потрапляє просто у template literal.
**Do:** будь-який lookup у мапу підписів закривати фолбеком `|| <дефолт>`, а при додаванні нового значення у контрол — грепати всі мапи, що на це значення дивляться.
**Seen:** 16.09.2026 · коміт aea453c
